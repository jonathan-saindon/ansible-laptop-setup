user_home: /home/$user
ansible_connection: local
aptcachetime: 3600
user:
    home: "{{ lookup('env','HOME') }}"
    name: jonathan
ssh:
  start: true
  service_enabled: true
  password_auth: false
  root_login: false
atom:
  config:
    filename: config.cson
    path: "{{ user.home }}/.atom/config.cson"
  packages:
    path: "conf/atom/packages.txt"
