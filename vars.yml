user_home: /home/$user
ansible_connection: local
aptcachetime: 3600

atom:
  config:
    src: "{{ conf.path }}/config.cson"
    dest: "{{ user.home }}/.atom/config.cson"
  packages:
    path: "{{ conf.path }}/atom/packages.txt"
bash:
  profile:
    src: "{{ conf.path }}/env/bash_profile"
    dest: "{{ user.home }}/.bash_profile"
conf:
  path: "{{ playbook_dir }}/conf"
ssh:
  start: true
  service_enabled: true
  password_auth: false
  root_login: false
user:
    home: "{{ lookup('env','HOME') }}"
    name: jonathan
